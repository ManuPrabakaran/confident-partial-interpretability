# Outputs

Experiment artifacts (JSON summaries, figures, tables) are written here.  
Synthetic demo default: `synthetic_demo.json` from `experiments/synthetic_demo.py`.

Do not treat files in this folder as peer-reviewed results unless the run is tied to a frozen protocol in `hypothesis.md` / `pre-registration_paper.md`.
